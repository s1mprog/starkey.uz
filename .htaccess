RewriteEngine On
RewriteBase /

# Если запрашиваемый файл/папка существует — отдать как есть
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Иначе — всегда отдаём index.html (SPA)
RewriteRule ^ index.html [L]
